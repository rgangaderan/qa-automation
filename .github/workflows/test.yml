name: Run workflow

on:
  push:
    branches:
      - master

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
    - name: Trigger second repository workflow
      uses: peter-evans/repository-dispatch@v1
      with:
        token: ${{ secrets.TOKEN }}
        repository: https://github.com/rgangaderan/qa-automation2.git
        event-type: run-workflow
